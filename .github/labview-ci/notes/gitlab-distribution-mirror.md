Adds a release-driven GitLab distribution mirror so GitLab projects can bootstrap the same canonical LabVIEW CI implementation without a second codebase. GitLab-source installs record their archive origin in the manifest, and later bootstrap updates re-fetch that origin instead of relying on a stale local installer; immutable release tags remain protected while channel aliases advance with the mirror.